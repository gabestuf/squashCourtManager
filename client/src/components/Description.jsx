import { Fragment } from "react";

const Description = () => {
  return (
    <div style={{ margin: "5rem" }}>
      <h2 style={{ margin: "1rem" }}>INFO</h2>
      <p>If the scores of a game do not go to 11, the game will not be counted as a win or loss for either player, but will still show up in match history. If you wish to delete a game, do so from the match history page. </p>
    </div>
  );
};

export default Description;
